<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.same_location"
    android:versionCode="1"
    android:versionName="1.0" >

    
    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen"
            android:name="com.example.same_location.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
          <activity
            android:name=".IndexFragment"
            android:label="@string/title_activity_index" >
        </activity>
        <activity
            android:name=".FindFragment"
            android:label="@string/title_activity_find" >
        </activity>
        <activity
            android:name=".MineFragment"
            android:label="@string/title_activity_mine" >
        </activity>
    </application>

</manifest>
